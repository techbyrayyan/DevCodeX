/** @type {import('next').NextConfig} */
const nextConfig = {
  // Disable the development indicator ("N" badge)
  devIndicators: false,

  // Compress responses
  compress: true,

  // Image optimization
  images: {
    // Modern formats — browser will pick AVIF or WebP automatically
    formats: ['image/avif', 'image/webp'],

    // Reasonable sizes for srcset generation
    deviceSizes: [640, 750, 828, 1080, 1200, 1920],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],

    // Aggressive caching — 30 days
    minimumCacheTTL: 2592000,
  },

  // Security and static caching headers
  async headers() {
    return [
      {
        source: '/:path*',
        headers: [
          {
            key: 'X-DNS-Prefetch-Control',
            value: 'on',
          },
          {
            key: 'Strict-Transport-Security',
            value: 'max-age=63072000; includeSubDomains; preload',
          },
          {
            key: 'X-XSS-Protection',
            value: '1; mode=block',
          },
          {
            key: 'X-Frame-Options',
            value: 'SAMEORIGIN',
          },
          {
            key: 'X-Content-Type-Options',
            value: 'nosniff',
          },
          {
            key: 'Referrer-Policy',
            value: 'origin-when-cross-origin',
          },
          {
            key: 'Permissions-Policy',
            value: 'camera=(), microphone=(), geolocation=()',
          },
        ],
      },
      {
        source: '/(.*\\.(?:png|jpg|jpeg|webp|avif|svg|ico|mp4|woff2))',
        headers: [
          {
            key: 'Cache-Control',
            value: 'public, max-age=31536000, immutable',
          },
        ],
      },
    ];
  },
};

export default nextConfig;
